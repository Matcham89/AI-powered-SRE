apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns-custom
  namespace: kube-system
data:
  platform-hosts.override: |
    template IN A local {
      match "^(auth|argocd|grafana|temporal)\.local\.$"
      answer "{{ .Name }} 60 IN A 192.168.5.15"
      fallthrough
    }
