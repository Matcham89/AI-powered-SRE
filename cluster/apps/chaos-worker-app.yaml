apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: chaos-worker
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    # After Temporal so the worker has a frontend to register with.
    argocd.argoproj.io/sync-wave: "6"
spec:
  project: default
  source:
    repoURL: https://github.com/Matcham89/AI-powered-SRE
    targetRevision: HEAD
    path: apps/chaos-worker
  destination:
    server: https://kubernetes.default.svc
    namespace: chaos-worker
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
