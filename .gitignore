# Age private key — NEVER commit this
keys.txt
*.age.key
age-key.txt

# SOPS environment variable exports
.sops-env

# Environment files
.env
.env.local
.env.*.local

# Kubeconfig — use KUBECONFIG env var instead
kubeconfig
kubeconfig.yaml
*.kubeconfig

# Helm chart cache
.helm/
charts/

# Terraform state (if used later)
*.tfstate
*.tfstate.backup
.terraform/

# macOS
.DS_Store

# Editor
.idea/
.vscode/
*.swp
*.swo

# Claude Code runtime artifacts — settings.local.json is personal, lock
# files are per-session runtime state; neither belongs in the repo.
.claude/scheduled_tasks.lock
.claude/settings.local.json
